import 'package:flutter/material.dart';

class Tab5View extends StatelessWidget {
  const Tab5View({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tab 5 Content'),
    );
  }
}
